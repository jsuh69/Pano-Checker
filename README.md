# Pano-Checker

M5StickS3 (ESP32-S3 기반) 모터 RPM 측정 및 주파수 분석기

## v0.5 주요 업데이트 내역 (최신 수정 사항)

### 1. BMI270 진동 센서 측정 모드 (VIB) 추가
* **진동 측정 모드 탑재**: 내장(혹은 핀 47, 48로 연결된) BMI270 가속도 센서를 이용해 모터의 물리적 진동 주파수를 직접 측정하는 `VIB` 모드를 새롭게 추가했습니다. 주변 소음이 너무 큰 환경에서도 진동을 통해 정확한 RPM 도출이 가능합니다.
* **센서 전환 UI 개선**: 짧은 버튼(BtnB) 클릭으로 감도 모드가 `LOW` -> `MID` -> `HIGH` -> `VIB` -> `LOW` 순서로 직관적으로 순환됩니다.

### 2. 고속 I2C 최적화 및 FFT 정밀 보정
* **독립 I2C 버스 할당 (Wire1)**: 기기의 전원 관리 칩(PMIC)과의 통신 충돌로 인해 기기가 먹통이 되거나 화면이 꺼지는 현상을 방지하기 위해 진동 센서 통신 전용으로 `Wire1` 버스(400kHz 고속)를 독립 할당했습니다.
* **실시간 샘플링 속도(Fs) 역산 적용**: I2C 통신 지연으로 인해 샘플링 속도가 변동되더라도, 실제로 측정된 시간(micros)을 기반으로 정확한 샘플링 주파수(actualFs)를 역산하여 FFT에 대입합니다. 이를 통해 지연이 발생해도 주파수 뻥튀기(예: 380Hz가 590Hz로 측정되는 현상) 없이 **무조건 정확한 물리적 주파수**를 계산해냅니다.
* **DC Offset 제거 및 Zero-padding 최적화**: 진동 센서의 기본 중력값(DC Offset)을 정확히 평균 내어 제거한 뒤 빈 공간을 0으로 채움으로써, FFT 변환 시 고주파 노이즈(Step-function artifact)가 폭증하는 현상을 완벽하게 제거했습니다.

### 3. 통합 오토 캘리브레이션 (Unified AUTO)
* 기존에 분리되었던 측정 모드별 자동 보정을 **하나의 `AUTO` 모드로 통합**했습니다.
* 사용자가 현재 어느 센서를 사용 중인지(마이크 또는 진동)를 스스로 기억하고, 길게 누르면 현재 활성화된 센서에 맞춰 자동으로 측정 감도 상하한선을 학습 및 적용합니다.

---

## v0.4 주요 업데이트 내역### 1. 강력한 주변 소음 필터링 (시간 & 강도 분석)
* **지속 시간 필터링 (0.3초 검증)**: 망치질이나 쇳소리 등 순간적인 큰 소음을 모터로 오인하지 않도록, 유효한 주파수 대역(150~750Hz)의 소리가 **0.3초(300ms) 이상 끊기지 않고 연속으로 유지될 때만** 진짜 모터 소리로 판정하는 로직을 추가했습니다.
* **소리 강도 상하한선(Amplitude Window) 적용**: 너무 작은 주변 소음뿐만 아니라 비정상적으로 큰 폭음(충격음)도 동시에 걸러내어, 순수한 모터 소리만의 일정한 볼륨 대역만 정확하게 잡아냅니다.

### 2. 현장 맞춤형 오토 캘리브레이션 (Auto-Calibration)
* **자동 임계값 학습**: 측정 환경이나 모터 종류, 거리가 달라지더라도 코드를 수정할 필요가 없도록 오토 캘리브레이션 기능을 탑재했습니다.
* **사용 방법 (BtnB 길게 누르기)**: 측정 위치에서 **버튼 B(오른쪽 버튼)를 1초 이상 길게 누르면** 화면에 "CALIBRATING"이 표시되며, 약 1.5초 동안 실제 모터 소리의 크기 평균을 학습합니다. 이후 이 소리 크기를 기준으로 측정 환경에 완벽하게 맞는 맞춤형 상하한선이 설정되고 **"AUTO"** 모드로 자동 진입합니다. (기존 LOW/MID/HIGH 모드 역시 짧은 클릭으로 계속 병행해서 사용할 수 있습니다.)

---

## v0.3 주요 업데이트 내역

### 1. 배터리 충전 안정성 개선 및 최적화
* **충전 사이클 정상화**: 기기가 켜진 상태에서 충전이 제대로 되지 않던 문제 해결. (PMIC의 충전 상태 머신이 매 루프마다 초기화되는 현상을 10초 타이머로 제어)
* **전압 측정 최적화**: 충전 시 배터리 전압이 비정상적으로 높게 측정되는 현상(IR Drop)을 우회하기 위해 정밀 측정 주기를 2분으로 조정하고, 전압 안정화 대기 시간을 3초로 단축시켜 배터리 효율 극대화.

### 2. 절전 기능 극대화 (스마트 슬립 모드)
* **오디오 및 연산 중지**: 설정된 시간 동안 입력이 없어 화면이 꺼질 경우, 무거운 FFT 주파수 분석 연산을 전면 중지하고 CPU를 대기(Idle) 모드로 전환하여 배터리 소모를 획기적으로 줄임.
* **마이크 전원 제어**: 절전 모드 진입 시 마이크 하드웨어를 아예 끄고(`M5.Mic.end()`), 기기를 깨울 때 다시 켜는(`M5.Mic.begin()`) 하드웨어 단위의 전력 제어 로직 적용.
* **스마트 버튼 UX**: 화면이 꺼져있는 상태에서 버튼(BtnA, BtnB)을 누르면 설정값이 변경되지 않고 기기를 깨우는 역할만 하도록 사용자 경험(UX) 개선.

### 3. 코드 구조 대대적 리팩토링 및 모듈화
* **상수(매직 넘버) 분리**: UI 색상, 배터리 관련 수치, 오디오 설정 등을 각각의 네임스페이스(`BatConfig`, `UI`, `Audio`, `PowerSave`)로 묶어서 가독성과 관리 편의성 증대.
* **배터리 매니저 클래스화**: 흩어져 있던 배터리 측정 변수 및 복잡한 타이머 로직들을 `BatteryManager` 클래스로 깔끔하게 캡슐화.
* **메모리 최적화**: RPM 숫자를 콤마(,)와 함께 포맷팅할 때, 메모리 파편화를 유발하던 잦은 `String` 객체 조작 대신 C-style 문자열(`char` 배열)을 활용해 최적화 달성.
